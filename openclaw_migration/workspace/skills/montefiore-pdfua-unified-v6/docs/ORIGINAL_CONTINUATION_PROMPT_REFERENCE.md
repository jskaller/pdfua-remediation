# Continuation Prompt Reference

Use the V6 quickstart packages as the controlling environment. Do not use older v1/v2/v3/v4/v5 packages as controlling if V6 is present.

Only process PDFs explicitly uploaded or named as active source PDFs. Examples are reference-only unless explicitly named as active.
