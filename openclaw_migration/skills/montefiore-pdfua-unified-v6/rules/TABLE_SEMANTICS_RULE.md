# Table Semantics Rule

Every data cell in a visible table must have a resolvable header association.

For non-trivial tables, use explicit `/ID` on TH cells and `/Headers` on TD cells where needed.

Run a table semantics/header association audit for visible/tagged tables before handoff.
